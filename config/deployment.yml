apiVersion: apps/v1
kind: Deployment
metadata:
  name: api
  labels:
    app: api
spec:
  replicas: 5
  selector:
    matchLabels:
      app: api
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  minReadySeconds: 5
  template:
    metadata:
      labels:
        app: api
    spec:
      containers:
      - name: api
        image: <IMAGE>
        ports:
        - containerPort: 80
        env:
          - name: DATABASE_URL
            valueFrom:
              secretKeyRef:
                name: database-secret
                key: database-url
---
apiVersion: v1
kind: Service
metadata:
  name: api-service
spec:
  type: LoadBalancer
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
    - name: loki
      protocol: TCP
      port: 81
      targetPort: 80
    - name: loki2
      protocol: TCP
      port: 82
      targetPort: 3000
    - name: loki3
      protocol: TCP
      port: 83
      targetPort: 3100 
    - name: loki4
      protocol: TCP
      port: 84
      targetPort: 7946 
  selector:
    app: api 